# 灵动视眸改进方案 v2.0

日期：2026年9月21日

定位：整合前期讨论的开发方案，用于团队分工、参赛实现和后续维护。
状态：方案，不代表下述功能已经完成；旧方案保留供对照。本文件未执行云服务器采购、GitHub建库或软件部署。

开发规则补充（2026年9月21日）：后续代码修改须从根目录 [AGENTS.md](AGENTS.md) 开始，并遵循 [开发实施与验收规则](docs/开发实施与验收规则.md)。其中细化了个人适配、模块边界、状态与权限、需求编号和测试门槛。规则不代表业务功能已实现。

## 一、最终定位与本次调整

建议项目名称保留“灵动视眸”，副标题调整为“眼控与语音融合的无障碍桌面任务助手”。目标用户是手部操作不便、但具备可用视线、面部动作或语音能力的使用者。语音是可选通道，不能成为完成核心任务的前提；不作医疗效果承诺。

不把项目做成“眼睛控制鼠标，加一个聊天框”。以文字型PDF阅读和资料整理为主场景，同时提供少量可靠的电脑操作：用户通过眼控或语音选择内容，系统理解任务、展示结果或执行预览，用户确认后完成保存等操作。

完整演示链路：个人校准 → 打开资料 → 粗选段落 → 放大并确认目标 → 眼控卡片或语音下达任务 → 本地/云端处理 → 核对出处和结果 → 确认保存。

| 方向 | 旧版方案重点 | 本版统一决定 |
|---|---|---|
| 眼控 | 调节灵敏度、多点校准、减少误触 | 保留为第一优先级，明确状态机和暂停安全机制 |
| 界面 | 阅读工作区、意图透镜 | 增加语音状态、模型路由状态和可访问的确认入口 |
| 语音 | 后续可选增强 | 纳入开发计划：离线识别为基础，联网识别可增强 |
| 智能体 | 主要调用DeepSeek | 规则、本地小模型、云端DeepSeek分工；执行器统一管控 |
| 本地模型 | 远期扩展 | 纳入增强版目标，先部署评测，再考虑微调 |
| 联网 | 接口与评审方式待明确 | 采用国内云服务器部署后端，客户端通过HTTPS访问 |
| 应用操作 | 主要限于内部文档任务 | 增加白名单应用和有限操作，不承诺任意软件全自动控制 |
| 交付维护 | 源码与安装包 | 在本人GitHub账户新建独立仓库，管理开发、版本和发布 |

首版承诺必须基于实际验收。若小模型或语音尚未达标，在参赛材料中标为实验功能或后续计划，不将路线图写成已有成果。

## 二、功能边界和开发优先级

### 2.1 提交版必须完成

- 稳定的眼控选择、明确暂停、误触抑制、校准与个人配置。
- 文字型PDF阅读、段落候选选择、意图透镜、大目标操作卡片。
- 国内公网可访问的后端，以及真实可用的DeepSeek文档处理链路。
- 摘要、时间事项提取、结果出处、修改与确认保存。
- 可取消、可超时处理、可断网降级的任务系统。
- 可运行的Windows安装包、使用说明、源码、测试记录。
- 独立GitHub仓库及基本版本管理。

### 2.2 同期争取完成，未通过测试不强行纳入承诺

- 离线语音识别和朗读；联网语音识别增强。
- 已登记应用的启动、切换及少量已验证操作。
- 一个约4B级量化模型的本地部署，用于意图理解和受限工具选择。
- 停留选择之外的扫描选择模式。

### 2.3 后续版本

模型微调、更多应用适配、扫描件OCR、跨设备同步、唤醒词、复杂多步桌面任务。暂不做任意软件自主操作、后台自动修改大量文件、自主执行模型生成脚本。

## 三、系统架构与本地/云端分工

建议客户端继续使用Python技术栈。先验证现有CustomTkinter能否支撑PDF工作区和大卡片交互；若确实不足，再选择迁移，不同时维护两套界面。后端可使用FastAPI，具体依赖版本在实现时锁定。

| 模块 | 运行位置 | 责任 |
|---|---|---|
| 摄像头与眼控 | 本地 | 关键点、校准、滤波、焦点、手势判断 |
| 语音采集与基础识别 | 本地 | 按需录音、离线转文字、停止与取消 |
| 规则路由器 | 本地 | 处理明确指令，如暂停、下一页、打开已登记应用 |
| 小模型 | 本地，可选 | 理解上下文、选择受限工具、缺少参数时询问 |
| 文档与执行器 | 本地 | 提供选中文本、校验参数、执行允许的操作 |
| 云后端 | 国内服务器 | 身份验证、限流、任务接口、密钥保管、接口代理 |
| DeepSeek | 后端调用 | 较复杂的摘要、问答、事项整理和任务理解 |
| 云端语音识别 | 后端调用，可选 | 对获授权的语音提供在线识别增强 |

统一执行路径：输入 → 意图判断 → 结构化动作建议 → 参数与权限校验 → 必要时用户确认 → 本地执行 → 结果反馈。

关键约束：

1. 眼控实时链路不经过大模型或云端，联网慢不能拖动光标。
2. 模型提出动作，程序决定能否执行；模型不能绕过权限和确认。
3. 简单指令不必经过小模型；不是每次云端请求都要再由小模型改写一次。
4. 云端回传结构化结果，不向客户端下发任意代码执行。
5. 暂停、停止、取消由本地优先处理，不能等待网络响应。

## 四、眼控精度与灵敏度改进

前期源码静态审阅发现：固定高增益、单点映射、单帧闭眼判断、双眼闭合直接双击、错误提示不完整，以及主动暂停可能被动作状态解除等风险。源码版本不一定与此前运行的EXE一致，应先复现再测量。

### 4.1 校准与特征

- 2026-09-22：固定提示区域，保留会话失效前诊断；映射引入训练点内波动惩罚并更新版本，旧档案须重校准。测试与限制见 `docs/校准失效与映射稳定性修复.md`。

- 2026-09-22戴镜排查补充：眼镜为正常使用条件。用户单独确认后可本地查看眼部裁剪及关键点，不保存上传；戴镜状态和实际采集尺寸进入档案环境匹配，分辨率选项只代表请求。详见 `docs/戴镜排查报告.md`，尚未完成真人戴镜验收。

- 2026-09-22批准采集质量审计及拟合失败细化：不要求完整校准，统计口径与测试边界见 `docs/采集质量审计报告.md`。原始画面预览未启用，候选仍只作对照。

- 2026-09-22批准补充：增加眼睑相对纵向特征的影子对照及训练质量前置检查，不自动切换控制算法。实现、实验门槛及测试边界见 `docs/纵向特征与训练检查报告.md`；训练通过仍须独立验证，真人精度尚待验收。

- 2026-09-22批准补充：五点校准前由用户/照护者确认舒适头部基准。显示参考与当前的简化轮廓、距离和倾斜提示；偏离则暂停当前点，回到范围后明确继续。可重建基准；不强迫标准坐姿，不以对齐替代独立验证。基准随有效档案保存，不保存原始画面。实现和真人验收情况见 `docs/头部基准优化报告.md`。

- 每位新用户首次眼控使用进入“首次适配向导”：位置检查、五点校准、独立验证、灵敏度试用、确认保存个人档案。校准解决位置映射，试用调节速度与稳定性，两者不能互相替代。
- 同一用户日常加载本人档案并做快速检查，无需每次完整校准；换人、换设备、显示配置或映射算法变化后重新验证，必要时重新校准。
- 未完成或未通过验证时禁止系统级眼控点击，但保留安全练习和可用的替代输入。不能要求用户先精准眼控点击才能开始校准。
- 核实所用模型的关键点定义，不直接把单个474号点等同于屏幕注视点。
- 使用虹膜区域估计和眼角局部坐标，加入双眼可用性判断。
- 眼睑开合采用尺度归一化特征，减少人与摄像头距离变化的影响。
- 五点校准作为起点，每点采集稳定片段并剔除异常值；用未参与拟合的位置验证。验证不通过允许重采，不虚报成功。
- 保留快速中心重新对齐，但不能将其称为完整校准。九点校准视实测需要增加。
- 记录可接受的头部位置范围；明显偏离时暂停或提示重新对齐。

### 4.2 运动与选择

- X/Y轴独立调节增益和死区，提供低灵敏度初始配置及个人保存。
- 先实现一种自适应滤波，与固定EMA对比，避免堆叠滤波导致迟滞。
- 主工作区采用“大目标区域选择”，不用精确像素点击作为唯一交互。
- 相邻候选不明确时放大候选让用户选择，而不是静默吸附到猜测目标。
- 自然短眨眼默认不触发双击；动作采用“候选—持续—触发一次—等待释放”的状态机。

### 4.3 安全与稳定

区分主动暂停、丢失跟踪和退出程序。主动暂停不能因为闭嘴或重新出现人脸而解除。恢复后清理旧动作和旧位置状态，避免光标突然跳动。

保留软件停止、键盘辅助停止和适合目标用户的可达停止方式；恢复并处理底层安全异常，不能只依赖鼠标移到屏幕角落。摄像头异常、模型缺失和线程失败必须可见，退出时释放摄像头和工作线程。

增益、停留时间和滤波参数通过基线测量确定，不承诺一组固定参数适合所有人。

## 五、界面与核心创新

### 5.1 意图透镜：从追求像素精度转向确认内容

用户粗略注视文本区域后，主动唤起透镜。系统放大少量候选段落，用户确认内容，再显示“总结、提取时间、朗读、返回”等固定大卡片。

文字型PDF利用文本块及页面坐标实现定位，维护缩放、滚动和屏幕坐标转换。首版不依赖大模型识别每帧屏幕，也不默认支持扫描件。

### 5.2 不确定目标澄清

当多个内容块都可能是目标时，明确展示候选；当语音指令缺少对象时，询问“是当前段落还是整页？”而不是直接执行。创新重点是可测量的误选减少，不是增加确认步骤本身。

### 5.3 能力适配界面

设置向导根据用户可用输入选择停留确认、扫描确认或语音辅助模板。用户可以手动覆盖设置。不要求每个人都能眨单眼、抬眉或清晰发音。

这属于受约束的交互模板适配，不宣称能自动重构所有第三方应用。

### 5.4 可检查的执行预览

文档任务显示原文页码、文本块、生成结果和保存位置。用户可修改、取消、确认。文件默认新建不覆盖，重复确认不重复保存。

预览是对受支持动作的检查机制，不是完整Windows模拟器，也不能保证撤销已发送的消息或其他外部操作。

### 5.5 界面布局

主界面包括阅读区、任务卡片、结果预览和常驻暂停/返回入口。摄像头预览可折叠；提供大字、高对比度、图标与文字双重提示。一次交互内卡片位置固定，不随视线逐帧移动。

显示当前为“离线可用”“云端可用”“正在识别”“等待确认”等实际状态，不用一个联网图标掩盖API故障。朗读、生成和录音各自可停止。

以上是拟验证的设计贡献，不能在未经检索和实验的情况下宣传为“市场上没有”或“全球首创”。

## 六、语音：离线可用，联网可增强

### 6.1 两条识别路径

离线：本地ASR将声音转换为文字，配合常用指令词表完成翻页、暂停、打开指定应用等操作。候选包括支持中文的Whisper小型版本或适合中文的离线识别方案，依据真实设备的准确率、延迟和许可证选型，不使用仅英文的模型替代中文方案。

在线：经用户授权后，通过自有后端调用国内语音识别服务，评估热词、应用名和噪声场景下的改善。联网不一定始终更准，需要与本地识别做同条件对比。

语音识别模型与语言模型是两类组件。DeepSeek文本API不能被默认当作直接接收音频的语音识别接口。

### 6.2 无障碍触发方式

默认采用“注视固定说话卡片并确认 → 提示音与明显录音状态 → 说话 → 静音结束或注视停止卡片”。不要求按住键盘或持续保持面部动作。

可选个人稳定动作触发；扫描模式可轮流高亮录音入口。唤醒词放在后续版本评估误唤醒和资源开销。不能发音的用户仍可通过眼控完成主流程。

### 6.3 避免输入互相干扰

- 录音期间禁用可能因说话误触的嘴部手势，保留独立安全停止。
- 朗读时防止扬声器声音被当成用户指令。
- 展示识别文本与最终动作，含糊时要求澄清。
- 每段语音使用唯一任务ID；本地与云端识别结果只能形成一次最终执行决定。
- 不允许本地先执行，云端晚到结果又执行一次。在线补识别应发生在执行前，并允许取消。

## 七、本地4B级模型：部署、职责与后续训练

### 7.1 概念说明

4B表示约40亿参数，不是4GB文件。4位量化表示以更低精度保存大部分权重，理论上40亿个4位权重约占2GB，但实际还包括量化元数据、未量化部分、运行时和上下文缓存，不能把2GB当成整机运行需求。

模型架构、预训练权重、推理引擎和智能体框架是不同东西。借用某种架构不会自动获得原模型能力。本项目应选择许可合适、权重可下载且可本地运行的小模型，不以未核实开放权重的GPT型号作为部署前提，也不需要接入GPT API。

### 7.2 本机部署策略

按前期配置记录：i9-14900HX、RTX 5060 Laptop约8GB显存、约16GB内存。实施前重新检查空闲资源和驱动；这些配置不代表已完成模型性能实测。

优先测试一个约4B级中文指令模型的4位量化版本，例如合适的Qwen系列开放权重模型。DeepSeek相关模型也需核对实际参数量、来源、许可和资源需求，不因同品牌就认为适合本机。

先用短上下文、单任务运行；可评估Ollama或llama.cpp，最终固定一种部署路线。推理服务只监听本机，不向公网暴露。视觉、语音和语言模型的资源竞争必须联合测试，必要时让轻量语音识别走CPU或按需加载小模型。

### 7.3 小模型负责什么

适合：将“帮我把这段记下来”转成结构化草稿任务；结合当前页面判断“下一页”的对象；从允许的工具中选择动作；缺少信息时询问；完成有限的短文本整理。

不保证：理解任意软件界面、稳定规划长任务、准确操作所有按钮、仅凭训练就获得可靠眼动定位。文本小模型本身不能看屏幕，需要程序提供结构化界面状态；视觉能力是另一项选型和测试任务。

所有简单且明确的控制仍优先使用规则。复杂长文任务交给云端DeepSeek，本地小模型不是每次操作都必经的中转站。

### 7.4 后续能否训练

可以在模型许可允许、训练工具兼容的条件下继续微调，但不建议从零训练，也不建议第一版就训练。路线为：

1. 部署现成模型，先测出错误类型。
2. 经同意收集并脱敏“指令、当前状态、允许工具、正确动作或澄清”的样本，加入拒绝危险操作和信息不足的反例。
3. 优先通过工具描述、规则、提示词和上下文管理解决问题。
4. 对仍存在的稳定问题尝试LoRA或QLoRA。
5. 在未参与训练的任务、用户表达或模板上验证；比较基线与微调模型。
6. 发布带版本和回滚能力的模型包，必要时重新量化并复测。

常见部署用GGUF量化文件不应直接视为通用训练输入。通常保留原始可训练权重来源，使用兼容训练方案，再转换成部署格式；QLoRA也需要受支持的训练栈。

8GB显存不等于保证能训练任意4B模型，训练需求受上下文、批量、优化器及实现影响。必要时租用GPU训练，训练后的模型仍可放回本机推理。普通云后端服务器与GPU训练服务器分开考虑。

“越用越聪明”不能靠自动收集全部点击和对话实现：原始操作可能有误，涉及隐私，必须审核、评测后更新。语音识别微调和眼控个体校准也不是语言模型微调。

## 八、电脑与应用操作能做到哪一步

| 等级 | 示例 | 本版安排 |
|---|---|---|
| 一级：项目内部操作 | 翻页、缩放、朗读、总结、提取、保存笔记 | 主链路，优先实现 |
| 二级：确定性系统操作 | 打开已登记应用、切换已识别窗口、调整音量 | 少量验证后纳入 |
| 三级：指定应用的固定流程 | 在已支持的文本编辑器填入已确认内容 | 逐个适配、逐个验收 |
| 四级：任意应用自主操作 | 自动理解陌生界面、自由执行复杂任务 | 不作为当前承诺 |

实现优先顺序：应用自有接口/系统接口 → Windows可访问性控件 → 已验证的快捷键 → 必要时图像或坐标定位。坐标点击容易受窗口、缩放和版本影响，不作为通用可靠方案。

工具示例：open_registered_app(app_id)、next_page(document_id)、draft_note(block_ids)、save_confirmed_note(draft_id)。参数使用登记ID和受限路径，不接受模型任意生成命令行。

打开程序不等于能操作其所有功能。删除文件、发送消息、付款、安装软件和更改系统安全设置默认不开放给模型；即使后续增加，也要单独设计权限、确认与审计。

## 九、国内联网与云服务器方案

### 9.1 确定路线

采用国内云服务器部署自己的后端，客户端通过HTTPS连接，后端调用DeepSeek和可选语音服务。阿里云、腾讯云等可作为候选，以可达性、学校资源、备案条件和预算确定供应商。

普通后端若只承担认证、转发和任务管理，可先按2核4GB量级做容量测试，不需要为此购买GPU服务器；这不是并发性能保证。优先小规模评测，按实际负载调整。

客户端只需向外访问HTTPS，不要求用户电脑公网IP、开放入站控制端口或使用VPN。GitHub只用于开发维护，不应成为安装包运行时必须访问的服务。

### 9.2 最小后端

- 评测账户或邀请码、短期访问凭据、撤销机制。
- 健康检查及服务可用性提示。
- 文档任务与语音识别代理接口。
- 参数与请求大小校验、频率限制、额度上限。
- 任务ID、取消、超时、失败反馈和脱敏日志。
- 模型供应商密钥保存在服务器端，不放进EXE、源码或前端配置。

首版不必做复杂社交、全量云盘和多端同步。个人眼控配置和笔记默认保存在本地；需要同步时再明确授权和数据范围。

### 9.3 部署与评审可达性

尽早落实域名、HTTPS和大陆节点相关备案要求；按实际服务性质及云厂商要求核验，不能以裸IP作为规避备案的方案。采购与备案需要用户账户和授权，方案本身不执行这些动作。

上线后用非校园网络和手机热点测试真实任务，而不是只检查网页能打开。分别验证后端可达、认证可用、DeepSeek调用和语音接口可用。评审账号不得依赖开发者现场登录，服务器和额度覆盖评审周期。

根据此前读取的要求，重点是作品涉及联网功能且具备公网联通性，不应擅自推导成必须改做纯网站。最终以组委会对桌面安装评测及公网入口的确认口径为准，可额外提供国内可访问的下载/说明页面。

### 9.4 网络错误与隐私

连接、等待响应和整体任务设独立超时；取消后丢弃晚到结果。重试不能重复执行本地保存等动作。服务故障时保留眼控、阅读和已安装的离线功能，并明确提示云端能力暂不可用。

摄像头画面留本地；上传文档只发用户授权的选段或范围；在线语音需明确录音和上传状态。录音默认不长期保留，日志不记录密钥和完整敏感内容。文档中的指令是待处理内容，不能被当成用户授权或系统指令执行。

## 十、在本人GitHub账户新建独立仓库

这是开发启动阶段的正式任务，不覆盖原仓库，也不把原项目改名当成全部创新。

### 10.1 建库前确认

1. 核对当前GitHub登录账户，确认确实属于用户。提供过源码链接不等于已验证账户归属。
2. 建议仓库名暂用`lingmou-assistant`，最终名称由用户确定或实施时确认。
3. 建议参赛研发期间设为私有，后续根据比赛规定和代码授权决定公开；未经确认不公开。
4. 记录旧项目来源、所用版本、许可证与本届新增内容。原代码缺少明确授权时先处理许可，不默认公开可见就可任意再分发。

本次仅将建库纳入方案，尚未创建GitHub仓库或推送代码。

### 10.2 推荐仓库结构

```text
lingmou-assistant/
  apps/desktop/          # 桌面UI、视觉、语音、文档、本地执行器
  services/api/          # 国内云端后端
  packages/contracts/   # 客户端与后端共享的数据协议
  tests/                # 状态机、工具权限、任务与接口测试
  configs/              # 无密钥的配置样例
  models/manifest/      # 模型来源、版本、许可、校验值；不是大权重文件
  scripts/              # 开发、构建、打包脚本
  docs/                 # 架构、开发、部署、使用、测试与参赛文档
  .github/workflows/    # 自动检查与构建
  .env.example
  .gitignore
  README.md
  CHANGELOG.md
  THIRD_PARTY_NOTICES.md
```

模型权重、大型安装包、API密钥、真实用户录音、人脸数据和私人文档不进入普通Git历史。模型与安装包采用单独发布存储；需再分发时核对许可证。提供校验值和模型清单，不能依赖开发者电脑上的绝对路径。

### 10.3 日常维护与发布

- `main`保留可运行版本；功能分支合并前通过检查，自动开发分支可使用`codex/功能名`。
- 提交说明写清新增、修复和影响；使用Issue记录任务与缺陷，使用PR说明改动和测试。
- 根据账户功能设置分支保护或等效合并约定，不直接依赖某项付费功能。
- 自动检查覆盖纯逻辑和模拟工具，默认不需要真实摄像头、云密钥和GPU。
- Windows打包另做专门验证，干净电脑测试不能由单元测试替代。
- 使用版本标签、更新日志、Release及文件校验值，明确源码版本与安装包对应关系。
- GitHub用于开发发行记录；面向国内评审可提供合规的国内下载镜像，安装运行不依赖GitHub实时可达。
- 误提交密钥时立即吊销并轮换，不能仅删除文件后继续使用原密钥。

## 十一、安装包与模型分发

基础安装包包含UI、必要运行依赖、眼控资源及明确配置的离线能力。离线语音模型若未内置，必须提供可离线安装的配套资源包，并在说明中准确区分“开箱离线”与“安装资源后离线”。

本地语言模型作为增强组件，可采用配套模型包或国内可访问的受控下载。首次运行检查硬件和校验值，模型未安装时不阻塞基础功能。不能要求评委自行安装Python、配置虚拟环境或临时访问境外模型站。

模型推理组件按需启动，退出和升级时妥善清理；对无独立显卡设备提供基础模式，不承诺相同推理性能。安装包不内置长期通用云端密钥，评测访问使用可限制额度的账户或凭据。

打包时检查第三方许可、资源路径、摄像头权限、中文路径、普通用户权限、卸载行为及杀毒误报情况。升级不得无提示删除用户配置和笔记。

## 十二、实施阶段与收缩策略

前期赛事文件中的节点包括9月28日登记、10月8日前相关省级材料、10月8—10日作品提交窗口，学校可能更早截止。这里沿用已提供材料，不代表已核实最新通知；先向学校确认实际截止。

按9月21日启动、2—3名有相关开发能力的成员持续投入估算，不保证工期：

| 阶段 | 时间建议 | 交付物 |
|---|---|---|
| 启动 | 9月21—22日 | 确认参赛资格、建新库、锁定依赖与资源、旧版基线；同时启动服务器与备案事项 |
| 控制与界面 | 9月23—25日 | 安全状态机、五点校准、灵敏度、PDF工作区和透镜原型 |
| 主链路 | 9月26—28日 | 国内后端、DeepSeek、出处与预览保存，首次端到端运行 |
| 输入与增强 | 9月29日—10月1日 | 离线语音、有限应用操作、小模型可行性测试；在线ASR按资源接入 |
| 冻结测试 | 10月2—4日 | 功能冻结、安装包、断网/超时/取消/误触测试和对比实验 |
| 交付 | 10月5—7日 | 修复阻断问题、视频、设计文档、源码与评测入口复核 |

后端可提前用键鼠界面联调，不必等待眼控全部完成。若备案或公网部署进度不足，尽早与学校协调已有合规服务条件，不到提交前才发现无法公网评测。

收缩顺序：先取消训练，再取消复杂应用适配，再推迟在线ASR和本地小模型增强；优先保住眼控、可访问界面、真实联网任务、结果确认和可运行安装包。若仅一人开发，需进一步压缩增强项。

## 十三、验收、实验与参赛表述

### 13.1 必测场景

- 自然眨眼、说话、暂时离开摄像头时不发生不应有的点击。
- 主动暂停后，闭嘴或人脸恢复不解除暂停。
- 校准失败可重试，滚动缩放后仍对应正确文档文本。
- 语音有歧义时不擅自操作，本地/云端结果不会重复执行。
- 文档含“忽略规则”等内容时，不提升其指令权限。
- 取消任务后晚到结果不执行；网络超时有反馈且不阻塞眼控。
- 未确认不保存，重复确认不重复保存，不覆盖原文。
- 云端不可用时离线核心功能仍能运行；模型未安装有明确说明。
- 在干净Windows环境与国内非校园网络完成安装和联网任务。
- 评测账户有效、额度足够、服务器持续可用。

### 13.2 需要收集的证据

比较旧版、控制修复版、加入透镜版；本地模型单独比较“规则/原模型/后续微调模型”，避免所有变化混在一起无法解释收益。

记录目标选择成功率、误触次数/分钟、任务完成时间、校准耗时、语音识别错误率及意图正确率、工具参数正确率、取消成功情况、模型耗时、峰值内存/显存、云端费用。

实验固定设备、目标尺寸和任务，安排练习并平衡顺序。训练集与测试集按表达模板、任务或用户适当隔离，不以重复样本证明泛化。没有目标用户参与时，明确只做了内部或模拟条件测试。

准确率、延迟和提升比例均用实测结果，不预写“99%精准”等宣传值。设计文档区分已完成、实验中、后续计划。

### 13.3 资格与交付

鉴于前期基础可能来自其他赛事，先向组委会确认改进后参赛的资格边界；新仓库、新名称和新增API不能自动消除重复参赛风险。保留来源、本届提交历史、成员贡献和版本差异。

交付包括：可安装版本与配套模型资源、源码及锁定依赖、部署和使用说明、校准指南、测试记录、第三方清单、本届模板设计文档、作品截图和演示视频。视频时长、大小、文件格式等以最终官方要求核对。

## 十四、启动开发前的最小确认清单

2026-09-22实验补充：戴镜纵向信号排查加入“上—中—下—中”固定水平短测，原始特征与过去0.20秒中位数进行配对对照。仅会话内标量数据，不自动接入控制、不放宽校准或验证门槛；实际精度与真人延迟待测。操作与边界见[上下短测说明](docs/上下短测说明.md)。

2026-09-22经一次真人戴镜短测批准下一实验：保留双眼平均基线，同时生成“横向双眼平均、纵向左眼”的100/150/200毫秒因果中位数候选。五点数据只训练候选；第一组三点只选型；第二组重新采集的三点才按原门槛作最终验收。候选选型结果不能复用为最终精度证据，失败仍锁定控制。旧映射档案因版本变化须重新校准。详见[混合映射与两阶段验收报告](docs/混合映射与两阶段验收报告.md)。

1. 用户GitHub账户、新库名称及私有/公开选择。
2. 赛事与学校实际截止，以及旧项目改进参赛的资格确认。
3. 国内云账户、预算、域名及备案可用条件。
4. 首批要支持的具体应用；建议先选1—2个易验证的应用。
5. 实际开发人数；据此决定本地小模型和在线ASR是否进入首次交付。

总体建议：先把“稳定眼控 + 文档意图透镜 + 国内联网DeepSeek + 确认执行”做扎实，以离线语音和本地小模型增加可用性，再用真实错误数据推动后续微调。创新价值来自用户能更可靠地完成任务，而不是接入模型的数量。
