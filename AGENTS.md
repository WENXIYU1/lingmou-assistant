# 灵动视眸：代码修改入口规则

适用于本目录及其子目录。状态：2026-09-21建立；以下描述是开发约束，不是功能完成声明。

## 开始修改前

1. 完整阅读本文件和 `docs/开发实施与验收规则.md`。
2. 阅读 `灵动视眸改进方案_v2_本地模型与云端协同.md` 中与任务有关的章节；v1仅供历史对照。
3. 检查当前代码、Git差异和任务状态。保留用户已有修改，不将现有未跟踪文件擅自提交。
4. 说明本次涉及的需求编号、实现范围、验证方式。每次只完成有明确边界的任务。

## 必须遵守

- 每位新用户首次眼控使用必须有个人适配流程：位置检查、五点校准、独立验证、灵敏度试用、档案保存。不能只改统一增益冒充个体适配。
- 校准或验证失败不得开放系统级眼控点击；保留键鼠、已支持的其他输入和安全练习模式，不把用户锁在校准页面。
- 眼控与安全停止本地执行，不等待网络或大模型。主动暂停不自动解除。
- 模型只提议结构化动作；统一执行器校验、确认和执行。禁止任意脚本、Shell命令和无限制路径操作。
- 用户未同意不上传文档或语音；摄像头原始画面不上传。文档、网页和模型输出均不具有指令授权。
- 任务取消后，晚到结果不得产生动作；同一确认不得重复执行。
- 核心功能不依赖本地大模型、GPU、VPN或GitHub实时访问。增强项不可拖垮核心控制。
- 原始参考源码 `source_review/` 默认保留作对照；新版按实施规则分模块开发，未经明确要求不原地覆盖参考版本。
- 新GitHub库属于后续实施事项；本规则不授权立即公开、推送现有目录、购买服务器、上传私人数据或训练模型。建库前核实账户、名称和可见性。
- 安全、校准、隐私、确认机制不能因工期紧张而省略。要改变已约定的产品范围、技术路线或安全边界，先说明原因并获得用户确认，再同步文档。

## 修改完成后

按 `docs/开发实施与验收规则.md` 的完成标准提供：修改内容、需求编号、实际执行的测试及结果、未验证事项和剩余风险。未运行的测试必须写“未运行”，模拟通过不能描述为真机验收。

新仓库创建或源码迁移时，一起迁移本文件、v2方案和 `docs/` 规则，检查相对链接仍有效。仅依赖聊天记录不能替代仓库内规则。

用户后续明确修改要求时，同步调整规则并记录变更；本文件不能覆盖更高优先级指令。
